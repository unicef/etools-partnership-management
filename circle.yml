machine:
   services:
     - docker

dependencies:
  cache_directories:
    - "~/dockercache"

  override:
    - if [[ -e ~/dockercache/image-pmp.tar ]]; then docker load -i ~/dockercache/image-pmp.tar; fi
    - docker build --rm=false -t unicef/etools-pmp:$CIRCLE_BRANCH .
    - if [[ -e ~/dockercache/image-pmp-test.tar ]]; then docker load -i ~/dockercache/image-pmp-test.tar; fi
    - docker build --rm=false -t unicef/etools-pmp-test:$CIRCLE_BRANCH --file docker-test .
    - mkdir -p ~/dockercache; docker save unicef/etools-pmp-test > ~/dockercache/image-pmp-test.tar; docker save unicef/etools-pmp > ~/dockercache/image-pmp.tar


test:
   override:
     - docker run -it unicef/etools-pmp-test:$CIRCLE_BRANCH

deployment:
   hub:
     branch: [develop, staging, master]
     commands:
       - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
       - docker push unicef/etools-pmp
